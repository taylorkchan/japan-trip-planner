{
  "name": "japan-trip-planner",
  "version": "1.0.0",
  "description": "Japan Trip Planner - Full Stack Application",
  "scripts": {
    "build": "cd 'Source Code/frontend' && npm install && npm run build",
    "dev": "concurrently \"npm run dev --prefix 'Source Code/frontend'\" \"npm run dev --prefix 'Source Code/backend'\"",
    "start": "cd 'Source Code/backend' && npm start"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}