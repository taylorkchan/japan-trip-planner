# Production Environment Configuration for Render
NODE_ENV=production

# Backend Configuration
USE_SUPABASE=false
PORT=10000

# Database (will be set by Render automatically)
# DATABASE_URL will be injected by Render from PostgreSQL service

# JWT Configuration
JWT_SECRET=your_production_jwt_secret_change_this
JWT_EXPIRES_IN=7d

# CORS Configuration
# CORS_ORIGIN will be set to frontend URL automatically by render.yaml

# API Keys (Optional - set these in Render dashboard for enhanced features)
# GOOGLE_MAPS_API_KEY=your_google_maps_api_key
# OPENWEATHER_API_KEY=your_openweather_api_key